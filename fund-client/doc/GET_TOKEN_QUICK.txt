╔════════════════════════════════════════════════════════════════╗
║         🔑 GET BEARER TOKEN - QUICK GUIDE                      ║
╚════════════════════════════════════════════════════════════════╝

✅ BACKEND IS RUNNING!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 METHOD 1: EASIEST - Use Swagger UI

  1. Visit: http://localhost:3001/api/docs

  2. Find "auth" section → Click "POST /auth/register"

  3. Click "Try it out"

  4. Fill in:
     {
       "email": "test@example.com",
       "name": "Test User",
       "password": "password123"
     }

  5. Click "Execute"

  6. Copy the "access_token" from response

  7. Click 🔓 "Authorize" button (top right)

  8. Paste token → Click "Authorize"

  ✅ Now you can test all protected endpoints!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 METHOD 2: Using Terminal Commands

STEP 1: Register a User
──────────────────────────────────────────────────────────────────
curl -X POST http://localhost:3001/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "name": "Test User",
    "password": "password123",
    "walletAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb"
  }'

Response will contain:
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": { ... }
}

COPY THE access_token VALUE!

STEP 2: Use Token in Requests
──────────────────────────────────────────────────────────────────
curl -X GET http://localhost:3001/auth/profile \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"

Replace YOUR_TOKEN_HERE with the access_token you copied!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 QUICK LOGIN (If Already Registered)

curl -X POST http://localhost:3001/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 SAVE TOKEN TO VARIABLE (Advanced)

# Register and save token
TOKEN=$(curl -s -X POST http://localhost:3001/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@test.com",
    "name": "User",
    "password": "pass123",
    "walletAddress": "0x123"
  }' | grep -o '"access_token":"[^"]*"' | cut -d'"' -f4)

# Use token
curl -X GET http://localhost:3001/auth/profile \
  -H "Authorization: Bearer $TOKEN"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 RECOMMENDED: Use Swagger UI

  It's visual, interactive, and easier!
  Visit: http://localhost:3001/api/docs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 Protected Endpoints (Need Token):

  ✅ GET    /auth/profile
  ✅ GET    /campaigns/my-campaigns
  ✅ POST   /campaigns
  ✅ PATCH  /campaigns/:id
  ✅ DELETE /campaigns/:id
  ✅ GET    /users
  ✅ POST   /funding
  ✅ GET    /funding/my-fundings

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 TOKEN FORMAT:

  Authorization: Bearer <token>
  
  Example:
  Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ EASIEST WAY: Open Swagger Now!

  http://localhost:3001/api/docs

════════════════════════════════════════════════════════════════

